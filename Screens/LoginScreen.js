import React from 'react';
import {
  View,
  Text,
  Image,
  TouchableOpacity,
  ScrollView,
  Dimensions,
  FlatList,
} from 'react-native';

const LoginScreen = () => {
    return (
      <Text>Login Screen</Text>
    );
  };


export default LoginScreen;
